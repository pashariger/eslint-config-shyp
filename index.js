module.exports = {
  rules: {
    // Enforce that strings are single-quoted, except for cases which would have
    // required escaping (e.g. "my name is 'sam'")
    quotes: [2, 'single', 'avoid-escape']
  }
};
